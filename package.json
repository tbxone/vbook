{
  "scripts": {
    "build": "npm run build:js && npm run build:css",
    "build:js": "terser src/vbook.js -o dist/vbook.min.js -c -m",
    "build:css": "postcss src/vbook.css -o dist/vbook.min.css --use cssnano"
  },
  "devDependencies": {
    "terser": "^5.36.0",
    "postcss-cli": "^11.0.0",
    "cssnano": "^7.0.0"
  }
}